# Dependencias para EJECUTAR el prototipo desplegado (app.py + caso.py +
# services/lstm_service.py, y lo que importan de RedBayesiana/codigo_red/).
# NO es un `pip freeze`: solo lo que ese código importa directamente. Las
# transitivas (scipy, starlette, etc.) las resuelve pip a partir de estas.
# Para entrenar modelos, correr pruebas de paridad o inspeccionar el
# notebook, usa `requirements-dev.txt` (instala también estas).
#
# Versiones fijadas exactamente a las ya usadas y validadas en el entorno
# `tesis-py311` (Python 3.11.3) — no se actualizó ningún paquete al
# escribir este archivo.

nicegui==3.17.1

pandas==2.2.3
numpy==2.2.6

tensorflow==2.21.0
keras==3.15.1

# Todavía no los importa ningún módulo de prototipo/ (ni app.py, ni
# caso.py, ni lstm_service.py) -- se necesitarán en cuanto exista
# bayes_service.py (tarjeta 3). Se documentan aquí, no en -dev, porque
# son dependencias de EJECUCIÓN del prototipo final, no de desarrollo.
pgmpy==1.1.2
networkx==3.4.2
